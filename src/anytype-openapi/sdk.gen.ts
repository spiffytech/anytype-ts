// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, Client, TDataShape } from './client';
import type { CreateApiKeyData, CreateApiKeyResponses, CreateApiKeyErrors, CreateAuthChallengeData, CreateAuthChallengeResponses, CreateAuthChallengeErrors, SearchGlobalData, SearchGlobalResponses, SearchGlobalErrors, ListSpacesData, ListSpacesResponses, ListSpacesErrors, CreateSpaceData, CreateSpaceResponses, CreateSpaceErrors, GetSpaceData, GetSpaceResponses, GetSpaceErrors, UpdateSpaceData, UpdateSpaceResponses, UpdateSpaceErrors, AddListObjectsData, AddListObjectsResponses, AddListObjectsErrors, RemoveListObjectData, RemoveListObjectResponses, RemoveListObjectErrors, GetListViewsData, GetListViewsResponses, GetListViewsErrors, GetListObjectsData, GetListObjectsResponses, GetListObjectsErrors, ListMembersData, ListMembersResponses, ListMembersErrors, GetMemberData, GetMemberResponses, GetMemberErrors, ListObjectsData, ListObjectsResponses, ListObjectsErrors, CreateObjectData, CreateObjectResponses, CreateObjectErrors, DeleteObjectData, DeleteObjectResponses, DeleteObjectErrors, GetObjectData, GetObjectResponses, GetObjectErrors, UpdateObjectData, UpdateObjectResponses, UpdateObjectErrors, ListPropertiesData, ListPropertiesResponses, ListPropertiesErrors, CreatePropertyData, CreatePropertyResponses, CreatePropertyErrors, DeletePropertyData, DeletePropertyResponses, DeletePropertyErrors, GetPropertyData, GetPropertyResponses, GetPropertyErrors, UpdatePropertyData, UpdatePropertyResponses, UpdatePropertyErrors, ListTagsData, ListTagsResponses, ListTagsErrors, CreateTagData, CreateTagResponses, CreateTagErrors, DeleteTagData, DeleteTagResponses, DeleteTagErrors, GetTagData, GetTagResponses, GetTagErrors, UpdateTagData, UpdateTagResponses, UpdateTagErrors, SearchSpaceData, SearchSpaceResponses, SearchSpaceErrors, ListTypesData, ListTypesResponses, ListTypesErrors, CreateTypeData, CreateTypeResponses, CreateTypeErrors, DeleteTypeData, DeleteTypeResponses, DeleteTypeErrors, GetTypeData, GetTypeResponses, GetTypeErrors, UpdateTypeData, UpdateTypeResponses, UpdateTypeErrors, ListTemplatesData, ListTemplatesResponses, ListTemplatesErrors, GetTemplateData, GetTemplateResponses, GetTemplateErrors } from './types.gen';
import { client } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Create API Key
 * After receiving a `challenge_id` from the `/v1/auth/challenges` endpoint, the client calls this endpoint to provide the corresponding 4-digit code along with the challenge ID. The endpoint verifies that the challenge solution is correct and, if it is, returns an `api_key`. This endpoint is central to the authentication process, as it validates the user's identity and issues a key that can be used for further interactions with the API.
 */
export const createApiKey = <ThrowOnError extends boolean = false>(options: Options<CreateApiKeyData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateApiKeyResponses, CreateApiKeyErrors, ThrowOnError>({
        url: '/v1/auth/api_keys',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Create Challenge
 * Generates a one-time authentication challenge for granting API access to the user's vault. Upon providing a valid `app_name`, the server issues a unique `challenge_id` and displays a 4-digit code within the Anytype Desktop. The `challenge_id` must then be used with the `/v1/auth/api_keys` endpoint to solve the challenge and retrieve an authentication token. This mechanism ensures that only trusted applications and authorized users gain access.
 */
export const createAuthChallenge = <ThrowOnError extends boolean = false>(options: Options<CreateAuthChallengeData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateAuthChallengeResponses, CreateAuthChallengeErrors, ThrowOnError>({
        url: '/v1/auth/challenges',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Search objects across all spaces
 * Executes a global search over all spaces accessible to the authenticated user. The request body must specify the `query` text (currently matching only name and snippet of an object), optional filters on types (e.g., "page", "task"), and sort directives (default: descending by last modified date). Pagination is controlled via `offset` and `limit` query parameters to facilitate lazy loading in client UIs. The response returns a unified list of matched objects with their metadata and properties.
 */
export const searchGlobal = <ThrowOnError extends boolean = false>(options: Options<SearchGlobalData, ThrowOnError>) => {
    return (options.client ?? client).post<SearchGlobalResponses, SearchGlobalErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/search',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List spaces
 * Retrieves a paginated list of all spaces that are accessible by the authenticated user. Each space record contains detailed information such as the space ID, name, icon (derived either from an emoji or image URL), and additional metadata. This endpoint is key to displaying a user’s workspaces.
 */
export const listSpaces = <ThrowOnError extends boolean = false>(options: Options<ListSpacesData, ThrowOnError>) => {
    return (options.client ?? client).get<ListSpacesResponses, ListSpacesErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces',
        ...options
    });
};

/**
 * Create space
 * Creates a new space based on a supplied name and description in the JSON request body. The endpoint is subject to rate limiting and automatically applies default configurations such as generating a random icon and initializing the workspace with default settings (for example, a default dashboard or home page). On success, the new space’s full metadata is returned, enabling the client to immediately switch context to the new internal.
 */
export const createSpace = <ThrowOnError extends boolean = false>(options: Options<CreateSpaceData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateSpaceResponses, CreateSpaceErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get space
 * Fetches full details about a single space identified by its space ID. The response includes metadata such as the space name, icon, and various workspace IDs (home, archive, profile, etc.). This detailed view supports use cases such as displaying space-specific settings.
 */
export const getSpace = <ThrowOnError extends boolean = false>(options: Options<GetSpaceData, ThrowOnError>) => {
    return (options.client ?? client).get<GetSpaceResponses, GetSpaceErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}',
        ...options
    });
};

/**
 * Update space
 * Updates the name or description of an existing space. The request body should contain the new name and/or description in JSON format. This endpoint is useful for renaming or rebranding a workspace without needing to recreate it. The updated space’s metadata is returned in the response.
 */
export const updateSpace = <ThrowOnError extends boolean = false>(options: Options<UpdateSpaceData, ThrowOnError>) => {
    return (options.client ?? client).patch<UpdateSpaceResponses, UpdateSpaceErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Add objects to list
 * Adds one or more objects to a specific list (collection only) by submitting a JSON array of object IDs. Upon success, the endpoint returns a confirmation message. This endpoint is vital for building user interfaces that allow drag‑and‑drop or multi‑select additions to collections, enabling users to dynamically manage their collections without needing to modify the underlying object data.
 */
export const addListObjects = <ThrowOnError extends boolean = false>(options: Options<AddListObjectsData, ThrowOnError>) => {
    return (options.client ?? client).post<AddListObjectsResponses, AddListObjectsErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/lists/{list_id}/objects',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Remove object from list
 * Removes a given object from the specified list (collection only) in a space. The endpoint takes the space, list, and object identifiers as path parameters and is subject to rate limiting. It is used for dynamically managing collections without affecting the underlying object data.
 */
export const removeListObject = <ThrowOnError extends boolean = false>(options: Options<RemoveListObjectData, ThrowOnError>) => {
    return (options.client ?? client).delete<RemoveListObjectResponses, RemoveListObjectErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/lists/{list_id}/objects/{object_id}',
        ...options
    });
};

/**
 * Get list views
 * Returns a paginated list of views defined for a specific list (query or collection) within a space. Each view includes details such as layout, applied filters, and sorting options, enabling clients to render the list according to user preferences and context. This endpoint is essential for applications that need to display lists in various formats (e.g., grid, table) or with different sorting/filtering criteria.
 */
export const getListViews = <ThrowOnError extends boolean = false>(options: Options<GetListViewsData, ThrowOnError>) => {
    return (options.client ?? client).get<GetListViewsResponses, GetListViewsErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/lists/{list_id}/views',
        ...options
    });
};

/**
 * Get objects in list
 * Returns a paginated list of objects associated with a specific list (query or collection) within a space. When a view ID is provided, the objects are filtered and sorted according to the view's configuration. If no view ID is specified, all list objects are returned without filtering and sorting. This endpoint helps clients to manage grouped objects (for example, tasks within a list) by returning information for each item of the list.
 */
export const getListObjects = <ThrowOnError extends boolean = false>(options: Options<GetListObjectsData, ThrowOnError>) => {
    return (options.client ?? client).get<GetListObjectsResponses, GetListObjectsErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/lists/{list_id}/views/{view_id}/objects',
        ...options
    });
};

/**
 * List members
 * Returns a paginated list of members belonging to the specified space. Each member record includes the member’s profile ID, name, icon (which may be derived from an emoji or image), network identity, global name, status (e.g. joining, active) and role (e.g. Viewer, Editor, Owner). This endpoint supports collaborative features by allowing clients to show who is in a space and manage access rights.
 */
export const listMembers = <ThrowOnError extends boolean = false>(options: Options<ListMembersData, ThrowOnError>) => {
    return (options.client ?? client).get<ListMembersResponses, ListMembersErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/members',
        ...options
    });
};

/**
 * Get member
 * Fetches detailed information about a single member within a space. The endpoint returns the member’s identifier, name, icon, identity, global name, status and role. The member_id path parameter can be provided as either the member's ID (starting  with `_participant`) or the member's identity. This is useful for user profile pages, permission management, and displaying member-specific information in collaborative environments.
 */
export const getMember = <ThrowOnError extends boolean = false>(options: Options<GetMemberData, ThrowOnError>) => {
    return (options.client ?? client).get<GetMemberResponses, GetMemberErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/members/{member_id}',
        ...options
    });
};

/**
 * List objects
 * Retrieves a paginated list of objects in the given space. The endpoint takes query parameters for pagination (offset and limit) and returns detailed data about each object including its ID, name, icon, type information, a snippet of the content (if applicable), layout, space ID, blocks and details. It is intended for building views where users can see all objects in a space at a glance.
 */
export const listObjects = <ThrowOnError extends boolean = false>(options: Options<ListObjectsData, ThrowOnError>) => {
    return (options.client ?? client).get<ListObjectsResponses, ListObjectsErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/objects',
        ...options
    });
};

/**
 * Create object
 * Creates a new object in the specified space using a JSON payload. The creation process is subject to rate limiting. The payload must include key details such as the object name, icon, description, body content (which may support Markdown), source URL (required for bookmark objects), template identifier, and the type_key (which is the non-unique identifier of the type of object to create). Post-creation, additional operations (like setting featured properties or fetching bookmark metadata) may occur. The endpoint then returns the full object data, ready for further interactions.
 */
export const createObject = <ThrowOnError extends boolean = false>(options: Options<CreateObjectData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateObjectResponses, CreateObjectErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/objects',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete object
 * This endpoint “deletes” an object by marking it as archived. The deletion process is performed safely and is subject to rate limiting. It returns the object’s details after it has been archived. Proper error handling is in place for situations such as when the object isn’t found or the deletion cannot be performed because of permission issues.
 */
export const deleteObject = <ThrowOnError extends boolean = false>(options: Options<DeleteObjectData, ThrowOnError>) => {
    return (options.client ?? client).delete<DeleteObjectResponses, DeleteObjectErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/objects/{object_id}',
        ...options
    });
};

/**
 * Get object
 * Fetches the full details of a single object identified by the object ID within the specified space. The response includes not only basic metadata (ID, name, icon, type) but also the complete set of blocks (which may include text, files, properties and dataviews) and extra details (such as timestamps and linked member information). This endpoint is essential when a client needs to render or edit the full object view.
 */
export const getObject = <ThrowOnError extends boolean = false>(options: Options<GetObjectData, ThrowOnError>) => {
    return (options.client ?? client).get<GetObjectResponses, GetObjectErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/objects/{object_id}',
        ...options
    });
};

/**
 * Update object
 * This endpoint updates an existing object in the specified space using a JSON payload. The update process is subject to rate limiting. The payload must include the details to be updated. The endpoint then returns the full object data, ready for further interactions.
 */
export const updateObject = <ThrowOnError extends boolean = false>(options: Options<UpdateObjectData, ThrowOnError>) => {
    return (options.client ?? client).patch<UpdateObjectResponses, UpdateObjectErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/objects/{object_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List properties
 * ⚠ Warning: Properties are experimental and may change in the next update. ⚠ Retrieves a paginated list of properties available within a specific space. Each property record includes its unique identifier, name and format. This information is essential for clients to understand the available properties for filtering or creating objects.
 */
export const listProperties = <ThrowOnError extends boolean = false>(options: Options<ListPropertiesData, ThrowOnError>) => {
    return (options.client ?? client).get<ListPropertiesResponses, ListPropertiesErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties',
        ...options
    });
};

/**
 * Create property
 * ⚠ Warning: Properties are experimental and may change in the next update. ⚠ Creates a new property in the specified space using a JSON payload. The creation process is subject to rate limiting. The payload must include property details such as the name and format. The endpoint then returns the full property data, ready for further interactions.
 */
export const createProperty = <ThrowOnError extends boolean = false>(options: Options<CreatePropertyData, ThrowOnError>) => {
    return (options.client ?? client).post<CreatePropertyResponses, CreatePropertyErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete property
 * ⚠ Warning: Properties are experimental and may change in the next update. ⚠ This endpoint “deletes” a property by marking it as archived. The deletion process is performed safely and is subject to rate limiting. It returns the property’s details after it has been archived. Proper error handling is in place for situations such as when the property isn’t found or the deletion cannot be performed because of permission issues.
 */
export const deleteProperty = <ThrowOnError extends boolean = false>(options: Options<DeletePropertyData, ThrowOnError>) => {
    return (options.client ?? client).delete<DeletePropertyResponses, DeletePropertyErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}',
        ...options
    });
};

/**
 * Get property
 * ⚠ Warning: Properties are experimental and may change in the next update. ⚠ Fetches detailed information about one specific property by its ID. This includes the property’s unique identifier, name and format. This detailed view assists clients in showing property options to users and in guiding the user interface (such as displaying appropriate input fields or selection options).
 */
export const getProperty = <ThrowOnError extends boolean = false>(options: Options<GetPropertyData, ThrowOnError>) => {
    return (options.client ?? client).get<GetPropertyResponses, GetPropertyErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}',
        ...options
    });
};

/**
 * Update property
 * ⚠ Warning: Properties are experimental and may change in the next update. ⚠ This endpoint updates an existing property in the specified space using a JSON payload. The update process is subject to rate limiting. The payload must include the name to be updated. The endpoint then returns the full property data, ready for further interactions.
 */
export const updateProperty = <ThrowOnError extends boolean = false>(options: Options<UpdatePropertyData, ThrowOnError>) => {
    return (options.client ?? client).patch<UpdatePropertyResponses, UpdatePropertyErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List tags
 * This endpoint retrieves a paginated list of tags available for a specific property within a space. Each tag record includes its unique identifier, name, and color. This information is essential for clients to display select or multi-select options to users when they are creating or editing objects. The endpoint also supports pagination through offset and limit parameters.
 */
export const listTags = <ThrowOnError extends boolean = false>(options: Options<ListTagsData, ThrowOnError>) => {
    return (options.client ?? client).get<ListTagsResponses, ListTagsErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}/tags',
        ...options
    });
};

/**
 * Create tag
 * This endpoint creates a new tag for a given property id in a space. The creation process is subject to rate limiting. The tag is identified by its unique identifier within the specified space. The request must include the tag's name and color. The response includes the tag's details such as its ID, name, and color. This is useful for clients when users want to add new tag options to a property.
 */
export const createTag = <ThrowOnError extends boolean = false>(options: Options<CreateTagData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateTagResponses, CreateTagErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}/tags',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete tag
 * This endpoint “deletes” a tag by marking it as archived. The deletion process is performed safely and is subject to rate limiting. It returns the tag’s details after it has been archived. Proper error handling is in place for situations such as when the tag isn’t found or the deletion cannot be performed because of permission issues.
 */
export const deleteTag = <ThrowOnError extends boolean = false>(options: Options<DeleteTagData, ThrowOnError>) => {
    return (options.client ?? client).delete<DeleteTagResponses, DeleteTagErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}/tags/{tag_id}',
        ...options
    });
};

/**
 * Get tag
 * This endpoint retrieves a tag for a given property id. The tag is identified by its unique identifier within the specified space. The response includes the tag's details such as its ID, name, and color. This is useful for clients to display or when editing a specific tag option.
 */
export const getTag = <ThrowOnError extends boolean = false>(options: Options<GetTagData, ThrowOnError>) => {
    return (options.client ?? client).get<GetTagResponses, GetTagErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}/tags/{tag_id}',
        ...options
    });
};

/**
 * Update tag
 * This endpoint updates a tag for a given property id in a space. The update process is subject to rate limiting. The tag is identified by its unique identifier within the specified space. The request must include the tag's name and color. The response includes the tag's details such as its ID, name, and color. This is useful for clients when users want to edit existing tags for a property.
 */
export const updateTag = <ThrowOnError extends boolean = false>(options: Options<UpdateTagData, ThrowOnError>) => {
    return (options.client ?? client).patch<UpdateTagResponses, UpdateTagErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/properties/{property_id}/tags/{tag_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Search objects within a space
 * Performs a search within a single space (specified by the `space_id` path parameter). Like the global search, it accepts pagination parameters and a JSON payload containing the search `query`, `types`, and sorting preferences. The search is limited to the provided space and returns a list of objects that match the query. This allows clients to implement space‑specific filtering without having to process extraneous results.
 */
export const searchSpace = <ThrowOnError extends boolean = false>(options: Options<SearchSpaceData, ThrowOnError>) => {
    return (options.client ?? client).post<SearchSpaceResponses, SearchSpaceErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/search',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List types
 * This endpoint retrieves a paginated list of types (e.g. 'Page', 'Note', 'Task') available within the specified space. Each type’s record includes its unique identifier, type key, display name, icon, and layout. While a type's id is truly unique, a type's key can be the same across spaces for known types, e.g. 'page' for 'Page'. Clients use this information when offering choices for object creation or for filtering objects by type through search.
 */
export const listTypes = <ThrowOnError extends boolean = false>(options: Options<ListTypesData, ThrowOnError>) => {
    return (options.client ?? client).get<ListTypesResponses, ListTypesErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types',
        ...options
    });
};

/**
 * Create type
 * Creates a new type in the specified space using a JSON payload. The creation process is subject to rate limiting. The payload must include type details such as the name, icon, and layout. The endpoint then returns the full type data, ready to be used for creating objects.
 */
export const createType = <ThrowOnError extends boolean = false>(options: Options<CreateTypeData, ThrowOnError>) => {
    return (options.client ?? client).post<CreateTypeResponses, CreateTypeErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete type
 * This endpoint “deletes” an type by marking it as archived. The deletion process is performed safely and is subject to rate limiting. It returns the type’s details after it has been archived. Proper error handling is in place for situations such as when the type isn’t found or the deletion cannot be performed because of permission issues.
 */
export const deleteType = <ThrowOnError extends boolean = false>(options: Options<DeleteTypeData, ThrowOnError>) => {
    return (options.client ?? client).delete<DeleteTypeResponses, DeleteTypeErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types/{type_id}',
        ...options
    });
};

/**
 * Get type
 * Fetches detailed information about one specific type by its ID. This includes the type’s unique key, name, icon, and layout. This detailed view assists clients in understanding the expected structure and style for objects of that type and in guiding the user interface (such as displaying appropriate icons or layout hints).
 */
export const getType = <ThrowOnError extends boolean = false>(options: Options<GetTypeData, ThrowOnError>) => {
    return (options.client ?? client).get<GetTypeResponses, GetTypeErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types/{type_id}',
        ...options
    });
};

/**
 * Update type
 * This endpoint updates an existing type in the specified space using a JSON payload. The update process is subject to rate limiting. The payload must include the name and properties to be updated. The endpoint then returns the full type data, ready for further interactions.
 */
export const updateType = <ThrowOnError extends boolean = false>(options: Options<UpdateTypeData, ThrowOnError>) => {
    return (options.client ?? client).patch<UpdateTypeResponses, UpdateTypeErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types/{type_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List templates
 * This endpoint returns a paginated list of templates that are associated with a specific type within a space. Templates provide pre‑configured structures for creating new objects. Each template record contains its identifier, name, and icon, so that clients can offer users a selection of templates when creating objects.
 */
export const listTemplates = <ThrowOnError extends boolean = false>(options: Options<ListTemplatesData, ThrowOnError>) => {
    return (options.client ?? client).get<ListTemplatesResponses, ListTemplatesErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types/{type_id}/templates',
        ...options
    });
};

/**
 * Get template
 * Fetches full details for one template associated with a particular type in a space. The response provides the template’s identifier, name, icon, and any other relevant metadata. This endpoint is useful when a client needs to preview or apply a template to prefill object creation fields.
 */
export const getTemplate = <ThrowOnError extends boolean = false>(options: Options<GetTemplateData, ThrowOnError>) => {
    return (options.client ?? client).get<GetTemplateResponses, GetTemplateErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/v1/spaces/{space_id}/types/{type_id}/templates/{template_id}',
        ...options
    });
};
